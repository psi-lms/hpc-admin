# Per-user state — regenerate locally with extract-users.py from the
# private data-lms repo. Never commit: this is a public repo and the
# file is a directory of all current LMS lab usernames.
scripts/USERS
